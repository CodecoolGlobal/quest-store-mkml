<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Quest store</title>
    <link rel="stylesheet" href="../static/assets/stylesheets/main.css">
    <link rel="stylesheet" href="../static/assets/stylesheets/quest-card.css">
</head>
<body onpageshow="progressBar()">
{% include "student_header.twig" %}
<section class="main-info cards-display">
    {% for quest in questList %}
        <div id = "{{ quest.getId }}" class="quest-card quest-card-basic">
            <div class="quest-card-header"> {{ quest.getName }}
                <div class="quest-card-price">
                    <div class="quest-card-price-box">{{ quest.getValue }}</div>
                </div>
            </div>
            <div class="quest-card-image">
                <img src="../static/assets/images/quest-card/quest-exploring-a-dungeon.jpeg" alt="dungeon image">
            </div>
            <div class="quest-card-category">{{ quest.getCategories.getName }}</div>
            <div class="quest-card-text-box">
                <div class="quest-card-text">{{ quest.getDescription }} </div>
                <button class="myButton">Claim</button>
            </div>
        </div>
    {% endfor %}
</section>
{% include "../../static/student/student-footer.html" %}
<script src="../static/Scripts/script.js"></script>
</body>
</html>